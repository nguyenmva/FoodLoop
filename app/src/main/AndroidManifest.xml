<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodLoop">
        <activity
            android:name=".ActiveRequests"
            android:exported="false" />
        <activity
            android:name=".Donation_Add_Page"
            android:exported="false" />
        <activity
            android:name=".Request_Home_Page"
            android:exported="false" />
        <activity
            android:name=".ActiveDonations"
            android:exported="false" />
        <activity
            android:name=".LogInScreen"
            android:exported="false" />
        <activity
            android:name=".LogOutScreen"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".ManageDonations" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".HistoryDonation" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".HistoryRequest" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".Profile"
            android:exported="false" />
        <activity
            android:name=".EditAccount"
            android:exported="false" />
        <activity
            android:name=".CreateAccount"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>